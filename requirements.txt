# ─────────────────────────────────────────────────────────────────────────────
# CAN Communication Suite — Python dependencies
# ─────────────────────────────────────────────────────────────────────────────
# Install with:
#	python -m venv venv
#	venv\Scripts\activate
#     pip install -r requirements.txt
#	py -m pip install -r requirements.txt

# GUI framework
PyQt5>=5.15.0

# ── Required for can_gui.py (real hardware) only ──────────────────────────
# Remove these two lines if you only use can_gui_dummy.py
python-can>=4.0.0
cantools>=39.0.0
